C program to calculate the area of a square 
#include <stdio.h>

int main() {
     int side, area;

    printf("Side: ");
	scanf("%d", &side);

	area=side*side;
	printf("Area of square: %d\n", area);

	return 0;
		
}
